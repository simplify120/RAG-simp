"""
Visualization suite for the LLM Text Simplification PE research project.

Modules:
    config       — Color palettes, metric metadata, publication style
    data_loader  — Data access layer (DB + CSV)
    plot_01_*    — Phase evolution (PE v1/v2 → RAG v2/v3)
    plot_02_*    — Model comparison vs T5 baseline
    plot_03_*    — Strategy analysis (Constraint / Structured / Zero-shot)
    plot_04_*    — Metric distributions across 40 texts
    plot_05_*    — Readability analysis (FKGL / FRE)
    run_all      — Master runner
"""
