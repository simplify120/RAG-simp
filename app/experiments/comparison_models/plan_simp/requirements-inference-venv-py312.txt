# =============================================================================
# Plan-Simp inference venv — Python 3.12+ (Windows-friendly wheels)
# =============================================================================
# NumPy 1.19 / torch 1.13 cannot install on Python 3.12 (no wheels; distutils
# removed). This file uses newer numpy/torch with prebuilt wheels.
#
# transformers==4.29.1 pulls tokenizers<0.14; those builds often have no
# cp312+Windows wheels, so pip compiles Rust (needs rustup). Use 4.38.x + newer
# tokenizers so pip installs binary wheels on Python 3.12 / Windows.
#
#   python -m venv .venv-plan-simp
#   source .venv-plan-simp/Scripts/activate   # Git Bash
#   python -m pip install -U pip setuptools wheel
#   pip install -r app/experiments/comparison_models/plan_simp/requirements-inference-venv-py312.txt
#
#   export PYTHONPATH="$(pwd)"
#   python -m app.experiments.comparison_models.plan_simp.run_plan_simp_pipeline
# =============================================================================

fire==0.3.1
nltk>=3.8,<4
numpy>=1.26.4,<3
pandas>=2.0,<3
sentence-transformers>=2.2.2,<4
transformers==4.38.2
tokenizers>=0.15.0,<0.19
pytorch-lightning==1.9.5
torch>=2.2.0,<3
scikit-learn>=1.3.0
tqdm>=4.64.0
